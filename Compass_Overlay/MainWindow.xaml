<Window x:Class="Compass_Overlay.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Compass_Overlay"
        mc:Ignorable="d"
        MouseDown="DragWindowWithLeftMouseButton"
        Topmost="True"
        WindowStyle="None" AllowsTransparency="True"
        SizeToContent="WidthAndHeight"
        Title="MainWindow" Height="144" Width="128">
    <Window.Background>
        <SolidColorBrush Color="Transparent" />
    </Window.Background>

    <Grid Margin="0,0,0,17" Width="129">
        <Grid.RowDefinitions>
            <RowDefinition Focusable="True" ToolTip="Options"/>
            <RowDefinition Height="Auto" ToolTip="Compass"/>
        </Grid.RowDefinitions>
        <Image HorizontalAlignment="Center" 
               VerticalAlignment="Top"
               Source="/compass2.png" Grid.RowSpan="2">
            <Image.ContextMenu>
                <ContextMenu>
                    <MenuItem Click="CloseButton_Click" Header="Close" />
                </ContextMenu>
            </Image.ContextMenu>
        </Image>
    </Grid>
</Window>
